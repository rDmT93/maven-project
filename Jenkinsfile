pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
  
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}